services:
  train:
    build: .
    image: ml-train:local
    volumes:
      - data_volume:/app/data
      - artifacts_volume:/app/artifacts
    command: python train.py --config config.yaml
volumes:
  data_volume:
  artifacts_volume:

    # Bind Mounts - for local binding of data and artifacts directories
    # volumes:
    #   - ./data:/app/data
    #   - ./artifacts:/app/artifacts

    # Named Volumes - for Docker-managed storage, artifacts persist in Docker storage
    # volumes:
    #   - data_volume:/app/data
    #   - artifacts_volume:/app/artifacts